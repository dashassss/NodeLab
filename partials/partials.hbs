
<h1>{{title}}</h1>

{{#if click}}
    <div id="container">
        {{#each list}}
            <div>
                <a href="/update?fio={{fio}}">fio: {{fio}}  number:{{number}}</a>
                </br>
            </div>
        {{/each}}
    </div>
{{else}}
    <div id="container">
        {{#each list}}
            <div>
                <a>fio: {{fio}}  number:{{number}}</a>
                </br>
            </div>
        {{/each}}
    </div>
{{/if}}
